allprojects {
    dependencies {
        implementation project(":domain")

        testImplementation(testFixtures(project(":domain")))
        testRuntimeOnly(testFixtures(project(":domain")))
    }
}