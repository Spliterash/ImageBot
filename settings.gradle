rootProject.name = 'image-bot'

include(
        'domain',

        'realization:image:commons',
        'realization:image:imageIO',
        'realization:pipelines:text',

        'realization:messengers:vk'
)