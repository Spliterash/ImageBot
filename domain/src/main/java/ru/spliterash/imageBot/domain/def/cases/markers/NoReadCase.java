package ru.spliterash.imageBot.domain.def.cases.markers;

/**
 * Маркер обозначающей, что кейс не читает данные, а только меняет их местами или делает ещё какую то херню, не вызывая инпут стримы
 */
public interface NoReadCase {
}
